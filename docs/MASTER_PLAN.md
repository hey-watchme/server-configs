# WatchMe マスタープラン（2026-07 再始動）

最終更新: 2026-07-05
Status: Active
Source of truth: プロダクトの目的・ゴール・そこに至るフェーズ計画の正本

## 0. 次にやること（次回セッションはここから）

**次回セッションの最初のタスク: AWS の新規アカウント開設**（2026-07-05、ユーザー決定）

- 通常は Phase 1（検証基盤/スイッチング層の構築）を先に進める順序だが、今回はインフラの土台（新環境）を先に作ることを選択した。Phase 1 は並行 or 後追いで進める
- 目的地は §4「新環境（東京）でのグリーンフィールド再構築」、作業項目は Phase 2 の1つ目（§5）
- やること:
  1. AWS の新規アカウントを開設する（既存アカウント 754724220380 とは別。理由: AIエージェント用IAMに広い権限を渡す前提のため、影響範囲を分離する）
  2. デフォルトリージョンを **ap-northeast-1（東京）** に設定
  3. アカウント作成後、このセクションの完了チェックを付け、Phase 2 の次の項目（AIエージェント用IAMの設計）に進む
- 前回セッションからの既知情報: 現行環境は ap-southeast-2（シドニー）、アカウント `754724220380`（IAMユーザー `watchme-api-user`、および `admin` プロファイル）。新環境ではこれらを参照用として残し、直接は触らない

進行中・未着手の他タスクは §5「フェーズ計画」の Phase 0/1 チェックリストを参照。

## 1. プロダクトの位置づけ（2026-07-05 確定）

WatchMe 本体（iOS アプリによる感情・環境・発話の分析）のゴールは、**プレゼンテーション用モックアップ**である。

- 目的は、投資家を驚かせ、仲間を集めること。一般ユーザー向けの実用サービスではない
- したがって**コストは度外視してよく、常に最高精度のモデル構成を選ぶ**
- 専用デバイスの開発は資金調達・チームアップ後。それまでは**このアプリで限界までチューニングしてビジョンを表現する**ことが最大の課題
- 音声から感情・気分・特性を高解像度で読み取る基盤技術は自社開発しない。世の中で最良のものをアプリケーションレイヤーで使う

### 入力の3経路（現行）

1. **アプリ内マイク録音** — 「録音→即分析→今の環境・感情・発話が見える」体験の核
2. **ウェアラブル議事録デバイス → Bluetooth → モバイル → ファイル分析** — 長時間モニタリング
3. **カメラロールの動画** — 音声抽出して分析

⚠️ Observer デバイス（ESP32、30分×48回の定点録音）は**凍結済み**。マイク性能とユースケース不在が理由。ドキュメントで Observer 前提の記述を見たら古い情報と判断すること。

## 2. ゴールの定義

以下の2つが達成されれば「あるべき姿」とする。

1. **表現力**: 1本の録音から「どんな環境で、何が起き、誰が何を話し、どんな感情だったか」が、見た人が驚くレベルの解像度と品質で可視化される
2. **堅牢性**: デモの場で絶対に壊れない。録音からダッシュボード表示まで、失敗しても状態が見え、リカバリーできる

## 3. 現在地（2026-07-05、DB実測に基づく）

- 実録音・実分析の最終稼働: **2026-04-23**（約2.5ヶ月凍結）
- **SER は死んでいる**: Hume Expression Measurement API が 2026-06-14 でサンセット（[TECH_INVENTORY_2026-07.md](./TECH_INVENTORY_2026-07.md) §2）。復旧ではなく再設計が必須
- ASR（Speechmatics）/ SED（AST）/ Profiler（gpt-5.4）は凍結時点まで稼働。ただし ASR/SED は最新世代との比較未実施
- **デモデータ生成 Lambda が毎時稼働継続中**（デモデバイスが spot_results に書き込み中）→ 停止/継続の判断が必要
- 既知の構造課題（KNOWN_ISSUES.md）: Lambda→EC2 のタイムアウト、監視の欠如、DLQ 蓄積、エンドポイント不統一
- 根本課題: **14 SQS キュー + 11 Lambda + 3並列パイプライン合流**という構成が、少人数デモ用途に対して過剰に複雑で、壊れやすさの主因

## 4. 設計原則とターゲットアーキテクチャ

### 設計原則1: モデルスイッチングのしやすさを最優先する（2026-07-05 決定）

前提認識:

- ベンチマーク・評判は、日本語の実音声を入れると全く当てにならないことが多い。**精度は使ってみないと一切分からない**
- 雑音耐性（路上等）もモデル次第で、事前に予測できない
- 評価軸の優先順位は **①話者分離（話者を特定できることが最重要。逐語精度がやや劣っても話者分離が完璧なモデルの方が有用）②雑音耐性 ③逐語精度**
- 新モデルは次々に出る

したがって「A/B テストで1つを選定して固定する」アプローチは取らない。**唯一の解決策はスイッチングのしやすさ**である。

- すべての分析ステージ（ASR / 音声理解LLM / プロファイリングLLM）にプロバイダー抽象化層を置き、設定1箇所で切替できる
- 新モデルの追加が「アダプタ1ファイルの追加」で済む構造にする
- 同一音源を複数モデルで並列実行し、結果を並べて比較できる検証モードを持つ
- 現行 Vibe Transcriber の `CURRENT_PROVIDER` 方式はこの思想の原型。ただし「切替に git push + デプロイが必要」なので、設定/DB による無停止切替まで進める

### 設計原則2: インフラは AI エージェントが運用する

- 当時は AWS コンソールを手で操作していたが、現在は MCP / CLI が充実している
- **AI エージェント用の IAM を作成し、構築・運用のかなりの部分をエージェントに任せる**
- 手作業のコンソール操作を前提にした構成・手順書は作らない

### 設計原則3: AWS 一辺倒にしない

- CDN・ストレージ等は Cloudflare（R2 / Workers 等）の方が使い勝手が良い場面が多く、採用を積極的に検討する
- 判断基準は「プレゼン用アプリを最小の複雑さで堅牢に動かせるか」

### ターゲット分析フロー（2系統ハイブリッド）

「3パイプライン並列 + SQS 合流」から、**2系統ハイブリッド**へ簡素化する。

```
録音（iOS: マイク / BTデバイス / 動画）
  │
  ├─ ① 専用ASR: 逐語文字起こし + 話者分離 + 単語タイムスタンプ（計測の骨格）
  │      初期ラインナップ: ElevenLabs Scribe v2 / AssemblyAI Universal-3 Pro / Speechmatics Melia-1
  │
  └─ ② マルチモーダルLLM（音声直接入力）: 感情・トーン・環境音・状況の構造化記述（解釈）
         初期ラインナップ: Gemini 3.1 Pro / Qwen3.5-Omni
  │
  └→ ③ 統合・プロファイリングLLM: ①+②を突合し、心理プロファイル + プレゼン品質のナラティブ生成
         初期ラインナップ: 最新フロンティア（GPT-5.x / Claude / Gemini）
```

- ①②③はいずれも「選定」ではなく「ラインナップ」。スイッチング層の上で常時入替・追加する
- 現行の SED（AST 527クラス）と SER（Hume）は ② に吸収して**リタイア**（固定クラスより自由記述の方が表現力が高く、プレゼン用途に合う）
- グラフ用の一貫スコア（vibe_score 等）が LLM 出力で安定しない場合のみ、emotion2vec+ / SenseVoice 等の専用モデルをラインナップに追加
- パイプライン段数が減る = 壊れる箇所が減る。合流待ち（aggregator-checker）が不要になり、SQS/Lambda を大幅削減できる

### 新環境（東京）でのグリーンフィールド再構築（2026-07-05 方針）

- 現行環境は **ap-southeast-2（シドニー）** にあり、日本からの利用で恒常的なレイテンシを生んでいる
- **「作り直す」前提で、東京リージョンに新環境をゼロから構築する**。現行シドニー環境はドキュメント・参照用の情報源と位置づけ、新環境の安定後に停止・削減する
- 新規 AWS アカウントを開設するか、既存アカウント内の新リージョンにするかは要判断（§6）。エージェント用 IAM に広い権限を渡す前提では、影響範囲を分離できる**新アカウントを推奨**
- Supabase・Cloudflare・外部AI API は新環境からも継続利用（Supabase プロジェクトのリージョンも合わせて再検討）

## 5. フェーズ計画

### Phase 0: 足場固め（今ここ）

- [x] 現状把握・技術棚卸し・ドキュメントのピン止め（2026-07-05）
- [x] デモデータ生成の停止（2026-07-05: EventBridge Scheduler `watchme-demo-data-generator-schedule` を DISABLED。再開は ENABLED に戻すだけ）
- [ ] AWS/外部サービスの棚卸し（シドニー環境で残す/止めるものの整理、課金の確認）
- [ ] 検証用テストセットの作成（日本語実録音: 静かな室内 / 生活雑音 / 路上、複数話者を含む）

### Phase 1: 検証基盤（スイッチング層）の構築

「選定」ではなく「いつでも比較・切替できる状態」を作ることがゴール。

- [ ] プロバイダー抽象化層の設計（ASR / 音声理解LLM / プロファイリングLLM の3ステージ共通の仕組み。無停止切替、アダプタ追加が容易）
- [ ] 初期ラインナップの実装（ASR 3種 + マルチモーダルLLM 2種から開始）
- [ ] 同一音源の並列実行・結果比較ができる検証ツール（CLI か簡易UI）
- [ ] テストセットでの初回検証（優先軸: 話者分離 > 雑音耐性 > 逐語精度）
- [ ] 出力スキーマ設計（モデル非依存の共通スキーマ。vibe_score の扱い、感情タクソノミーの再設計）

### Phase 2: 新環境（東京）の構築

- [ ] 新 AWS アカウント開設（東京リージョン、既存アカウント 754724220380 とは分離）← **次回セッションの最初のタスク（§0）**
- [ ] AI エージェント用 IAM の設計・作成（エージェントによる構築・運用を前提）
- [ ] 最小構成の2系統パイプラインを新環境に構築（ストレージ等は Cloudflare 採用も比較検討）
- [ ] 処理状態の可視化（アプリから「いまどの段階か・失敗したか」が見える）と失敗時の再実行手段
- [ ] 最低限の監視・アラート
- [ ] シドニー環境を参照専用に凍結 → 新環境安定後に停止・削減

### Phase 3: プレゼン品質のチューニング（iOS）

- [ ] 新しいリッチな分析出力（環境の自由記述・文脈込み感情）を活かした可視化の再設計
- [ ] 「録音 → 進行表示 → 結果が現れる」というデモの流れの磨き込み
- [ ] 既知バグ潰し（深夜日付ロールオーバー、decode 失敗で当日表示全滅、認証トークン等）
- [ ] デモシナリオ（投資家向けに見せる録音・ストーリー）の整備

## 6. 未決事項（ユーザー判断待ち）

| # | 論点 | 選択肢 |
|---|------|--------|
| 1 | Cloudflare 等の採用範囲 | ストレージ(R2)・CDN のみ / 処理系(Workers/Queues)まで / AWS 継続 |
| 2 | SED/SER のリタイア範囲 | 完全に LLM へ置換 / スコア安定用に専用モデル併用（Phase 1 の検証結果で判断） |
| 3 | 旧 Observer 系・シドニー資産 | 停止・削減の時期と範囲（Phase 2 終盤で判断） |

解決済み:

- ~~デモデータ生成 Lambda~~ → 2026-07-05 停止（DISABLED、可逆）
- ~~新環境のアカウント~~ → 2026-07-05 決定: 新規 AWS アカウントを開設する（既存 754724220380 とは分離、東京リージョン）。§0 参照

## 7. 更新ルール

- フェーズの完了・方針変更のたびにこの文書を更新する
- 技術選定の根拠は [TECH_INVENTORY_2026-07.md](./TECH_INVENTORY_2026-07.md) に残す（次回棚卸し時は新しい日付でスナップショットを作る）
